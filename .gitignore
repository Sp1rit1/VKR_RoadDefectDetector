# Виртуальное окружение Python
.venv/
.venv_py310/
venv/
ENV/
env/
# Добавь конкретное имя твоего venv, если оно другое, например:
# venv_yolo/

# Кэш и скомпилированные файлы Python
__pycache__/
*.py[cod]
*$py.class
*.egg
*.egg-info/
dist/
build/
eggs/
parts/
bin/ # Часто часть venv или build артефактов
include/ # Часто часть venv
lib/ # Часто часть venv
lib64/ # Часто часть venv
share/ # Часто часть venv
wheels/
pip-wheel-metadata/
develop-eggs/
sdist/
var/

# Папки с данными - ИСКЛЮЧАЕМ ПОЛНОСТЬЮ
data/

# Обученные модели (веса) - ИСКЛЮЧАЕМ ПОЛНОСТЬЮ
weights/

# Логи TensorBoard и другие логи - ИСКЛЮЧАЕМ ПОЛНОСТЬЮ
logs/
*.log
*.log.*

# Результаты работы скриптов / предсказаний - ИСКЛЮЧАЕМ ПОЛНОСТЬЮ
outputs/
results/

# Файлы IDE и редакторов
.idea/            # IntelliJ, PyCharm
.vscode/          # VS Code
*.sublime-workspace
*.sublime-project
nbproject/        # NetBeans
*.swp             # Vim swap files
*~                # Backup files from some editors
*.bak
*.tmp

# Специфичные для ОС файлы
.DS_Store         # macOS
Thumbs.db         # Windows
ehthumbs.db       # Windows
Desktop.ini       # Windows

# Jupyter Notebooks (если не хочешь хранить output ячеек или сами ноутбуки большие)
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/ # Если ноутбуки в папке notebooks
# Если сами файлы .ipynb очень большие из-за вывода, и их много:
# notebooks/*.ipynb
# Но обычно сами .ipynb файлы (без output) хранят. Если папка notebooks/ содержит только .ipynb,
# и они не огромные, можно ее не добавлять в .gitignore, а если она содержит данные или большие выводы, то добавить:
# notebooks/

# Специфичные для сборки и тестирования
coverage.xml
.coverage
htmlcov/
MANIFEST
*.mo
*.pot